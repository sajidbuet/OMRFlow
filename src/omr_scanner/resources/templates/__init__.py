"""Packaged document templates handed to the user on request.

Currently the candidate/attendance sample workbook offered by the Attendance
stage (Phase 7). Read through :mod:`importlib.resources` so it resolves in a
wheel and a frozen build, never by a path relative to the repository.
"""
